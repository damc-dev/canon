{
  "mcpServers": {
    "canon": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${CLAUDE_PLUGIN_ROOT}",
        "--frozen",
        "canon-mcp"
      ]
    }
  }
}

{
  "mcpServers": {
    "canon": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "--frozen",
        "canon-mcp"
      ]
    }
  }
}
